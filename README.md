# Virtual Code Ninjas — Next.js Starter Repo

This repository contains starter code, test harnesses, and teaching materials for the Junior Ninjas (ages 9–12) JavaScript + Game Dev cohort.

Run tests:

```bash
npm install
npm test
```
<!-- 
echo $SHELL
nano ~/.bash_profile

brew unlink node
brew install node@22
brew link node@22 --force --overwrite
node --version

nvm install 22
nvm use 22
nvm alias default 22


  git add .
   git commit -m "Modified the look and Feel of the application"
   git push
   Deploy to Fly.io:
The GitHub Action should automatically deploy, or you can manually deploy from ws-server:
cd ws-server
fly deploy
 -->
